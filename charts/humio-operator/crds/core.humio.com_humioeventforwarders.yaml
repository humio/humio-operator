---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: humioeventforwarders.core.humio.com
  labels:
    app: 'humio-operator'
    app.kubernetes.io/name: 'humio-operator'
    app.kubernetes.io/instance: 'humio-operator'
    app.kubernetes.io/managed-by: 'Helm'
    helm.sh/chart: 'humio-operator-0.0.1'
spec:
  group: core.humio.com
  names:
    categories:
    - humio
    - all
    kind: HumioEventForwarder
    listKind: HumioEventForwarderList
    plural: humioeventforwarders
    shortNames:
    - heff
    singular: humioeventforwarder
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Ready status
      jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - description: Forwarder type
      jsonPath: .spec.forwarderType
      name: Type
      type: string
    - description: Kafka topic (if kafka type)
      jsonPath: .spec.kafkaConfig.topic
      name: Topic
      type: string
    - description: Whether forwarder is enabled
      jsonPath: .spec.enabled
      name: Enabled
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          HumioEventForwarder is the Schema for the humioeventforwarders API.
          It manages event forwarder destinations in LogScale where events can be forwarded.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: HumioEventForwarderSpec defines the desired state of HumioEventForwarder.
            properties:
              description:
                description: Description of the event forwarder.
                type: string
              enabled:
                default: true
                description: |-
                  Enabled controls whether the forwarder is active.
                  When disabled, events will not be forwarded to this destination.
                type: boolean
              externalClusterName:
                description: |-
                  ExternalClusterName refers to an object of type HumioExternalCluster where the Humio resources should be created.
                  This conflicts with ManagedClusterName.
                minLength: 1
                type: string
              forwarderType:
                default: kafka
                description: |-
                  ForwarderType specifies the type of forwarder.
                  Currently only "kafka" is supported.
                enum:
                - kafka
                type: string
              kafkaConfig:
                description: |-
                  KafkaConfig contains Kafka-specific configuration.
                  Required when forwarderType is "kafka".
                properties:
                  properties:
                    description: |-
                      Properties contains non-sensitive Kafka configuration in x.y.z=abc format.
                      Newline-separated list of properties.
                      For sensitive properties (passwords, keys), use PropertiesSecretRef instead.
                      See: https://library.humio.com/falcon-logscale-self-hosted/ingesting-data-event-forwarders.html#ingesting-data-event-forwarders-kafka
                      Example:
                        bootstrap.servers=kafka.example.com:9092
                        security.protocol=SASL_SSL
                        sasl.mechanism=PLAIN
                    maxLength: 1048576
                    type: string
                  propertiesSecretRef:
                    description: |-
                      PropertiesSecretRef references a Kubernetes Secret containing sensitive Kafka properties.
                      The secret data should be in the same x.y.z=abc format as Properties.
                      Properties from both fields are merged, with PropertiesSecretRef taking precedence.
                      This is the recommended way to provide sensitive configuration like passwords and SSL keys.
                    properties:
                      key:
                        description: Key within the Secret containing the data.
                        minLength: 1
                        type: string
                      name:
                        description: Name of the Secret.
                        minLength: 1
                        type: string
                      namespace:
                        description: Namespace of the Secret. If empty, defaults to
                          the same namespace as the HumioEventForwarder.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  topic:
                    description: Topic is the Kafka topic to forward events to.
                    minLength: 1
                    type: string
                required:
                - topic
                type: object
                x-kubernetes-validations:
                - message: At least one of properties or propertiesSecretRef must
                    be specified
                  rule: has(self.properties) || has(self.propertiesSecretRef)
              managedClusterName:
                description: |-
                  ManagedClusterName refers to an object of type HumioCluster that is managed by the operator where the Humio
                  resources should be created.
                  This conflicts with ExternalClusterName.
                minLength: 1
                type: string
              name:
                description: |-
                  Name of the event forwarder in LogScale.
                  This is the human-readable name that will be displayed in the UI.
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: Value is immutable
                  rule: self == oldSelf
            required:
            - description
            - enabled
            - forwarderType
            - name
            type: object
            x-kubernetes-validations:
            - message: Must specify exactly one of managedClusterName or externalClusterName
              rule: (has(self.managedClusterName) && self.managedClusterName != "")
                != (has(self.externalClusterName) && self.externalClusterName != "")
            - message: kafkaConfig is required when forwarderType is kafka
              rule: self.forwarderType != 'kafka' || has(self.kafkaConfig)
          status:
            description: HumioEventForwarderStatus defines the observed state of HumioEventForwarder.
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the forwarder's state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              eventForwarderID:
                description: |-
                  EventForwarderID is the LogScale-generated ID for this forwarder.
                  This ID is used internally by LogScale to reference the forwarder.
                type: string
              managedByOperator:
                description: |-
                  ManagedByOperator indicates whether the operator successfully owns this LogScale event forwarder.
                  Set to true when the operator creates a new forwarder or successfully adopts an existing one.
                  Set to false when adoption is rejected.
                  When false, the finalizer will not delete the forwarder from LogScale.
                type: boolean
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
