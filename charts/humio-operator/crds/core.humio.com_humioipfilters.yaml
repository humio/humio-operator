---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: humioipfilters.core.humio.com
  labels:
    app: 'humio-operator'
    app.kubernetes.io/name: 'humio-operator'
    app.kubernetes.io/instance: 'humio-operator'
    app.kubernetes.io/managed-by: 'Helm'
    helm.sh/chart: 'humio-operator-0.31.1'
spec:
  group: core.humio.com
  names:
    kind: HumioIPFilter
    listKind: HumioIPFilterList
    plural: humioipfilters
    singular: humioipfilter
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: The state of the IPFilter
      jsonPath: .status.state
      name: State
      type: string
    - description: Humio generated ID
      jsonPath: .status.id
      name: HumioID
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: HumioIPFilter is the Schema for the humioipfilters API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: HumioIPFilterSpec defines the desired state of HumioIPFilter
            properties:
              externalClusterName:
                description: |-
                  ExternalClusterName refers to an object of type HumioExternalCluster where the Humio resources should be created.
                  This conflicts with ManagedClusterName.
                minLength: 1
                type: string
              ipFilter:
                description: IPFilter is a list of firewall rules that define access
                  control for IP addresses and subnets
                items:
                  description: FirewallRule defines action/address pairs
                  properties:
                    action:
                      description: Action determines whether to allow or deny traffic
                        from/to the specified address
                      enum:
                      - allow
                      - deny
                      type: string
                    address:
                      description: Address specifies the IP address, CIDR subnet,
                        or "all" to which the Action applies
                      pattern: ^(all|((25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)\.){3}(25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)(/(3[0-2]|[12]?[0-9]))?|([0-9a-fA-F]{1,4}:){7}[0-9a-fA-F]{1,4}(/([0-9]|[1-9][0-9]|1[0-1][0-9]|12[0-8]))?|::1(/([0-9]|[1-9][0-9]|1[0-1][0-9]|12[0-8]))?|::(/([0-9]|[1-9][0-9]|1[0-1][0-9]|12[0-8]))?)$
                      type: string
                  required:
                  - action
                  - address
                  type: object
                minItems: 1
                type: array
              managedClusterName:
                description: |-
                  ManagedClusterName refers to an object of type HumioCluster that is managed by the operator where the Humio resources should be created.
                  This conflicts with ExternalClusterName.
                minLength: 1
                type: string
              name:
                description: Name for the IPFilter within Humio (immutable after creation)
                maxLength: 253
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: Value is immutable
                  rule: self == oldSelf
            required:
            - ipFilter
            - name
            type: object
            x-kubernetes-validations:
            - message: Must specify exactly one of managedClusterName or externalClusterName
              rule: (has(self.managedClusterName) && self.managedClusterName != "")
                != (has(self.externalClusterName) && self.externalClusterName != "")
          status:
            description: HumioIPFilterStatus defines the observed state of HumioIPFilter.
            properties:
              id:
                description: ID stores the Humio generated ID for the filter
                type: string
              state:
                description: State reflects the current state of the HumioIPFilter
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
