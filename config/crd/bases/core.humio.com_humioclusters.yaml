
---
apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.2.2
  creationTimestamp: null
  name: humioclusters.core.humio.com
spec:
  group: core.humio.com
  names:
    kind: HumioCluster
    listKind: HumioClusterList
    plural: humioclusters
    singular: humiocluster
  scope: ""
  subresources:
    status: {}
  validation:
    openAPIV3Schema:
      description: HumioCluster is the Schema for the humioclusters API
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/api-conventions.md#resources'
          type: string
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/api-conventions.md#types-kinds'
          type: string
        metadata:
          type: object
        spec:
          description: HumioClusterSpec defines the desired state of HumioCluster
          properties:
            image:
              description: Desired container image
              type: string
            nodePools:
              description: Desired number of nodes
              items:
                description: HumioNodePool defines a pool of nodes with equal configuration
                properties:
                  firstNodeID:
                    description: The ID to the first node in the pool
                    type: integer
                  name:
                    description: The name of the node pool
                    type: string
                  nodeCount:
                    description: Desired number of nodes
                    type: integer
                  nodeDiskSizeGB:
                    description: Desired disk size of each node
                    type: integer
                  types:
                    description: The node types for the node pool
                    items:
                      description: HumioNodeType will define which responsibilities
                        nodes will take on.
                      type: string
                    type: array
                type: object
              type: array
            singleUserPassword:
              description: Password for the default user 'developer'
              type: string
            targetReplicationFactor:
              description: Desired number of replicas of both storage and ingest partitions
              type: integer
            version:
              description: Desired version of Humio nodes
              type: string
          type: object
        status:
          description: HumioClusterStatus defines the observed state of HumioCluster
          properties:
            allDataAvailable:
              description: Current state set by operator.
              type: string
            baseURL:
              description: BaseURL pointing to an endpoint for the cluster
              type: string
            jwtToken:
              description: JWTToken for interacting with the cluster.
              type: string
            stateLastUpdated:
              format: int64
              type: integer
          type: object
      type: object
  version: v1alpha1
  versions:
  - name: v1alpha1
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
