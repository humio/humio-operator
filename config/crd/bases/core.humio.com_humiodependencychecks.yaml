---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: humiodependencychecks.core.humio.com
  labels:
    app: 'humio-operator'
    app.kubernetes.io/name: 'humio-operator'
    app.kubernetes.io/instance: 'humio-operator'
    app.kubernetes.io/managed-by: 'Helm'
    helm.sh/chart: 'humio-operator-0.0.1'
spec:
  group: core.humio.com
  names:
    categories:
    - humio
    - all
    kind: HumioDependencyCheck
    listKind: HumioDependencyCheckList
    plural: humiodependencychecks
    shortNames:
    - hdc
    singular: humiodependencycheck
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: The type of dependency check
      jsonPath: .spec.checkType
      name: CheckType
      type: string
    - description: The HumioCluster this check belongs to
      jsonPath: .spec.managedClusterName
      name: Cluster
      type: string
    - description: How the check was discovered
      jsonPath: .status.discoverySource
      name: Source
      type: string
    - jsonPath: .status.conditions[?(@.type=='Configured')].status
      name: Configured
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: HumioDependencyCheck is the Schema for the humiodependencychecks
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: HumioDependencyCheckSpec defines the desired state of HumioDependencyCheck
            properties:
              checkType:
                description: CheckType is the type of dependency check (kafka, s3,
                  gcs)
                enum:
                - kafka
                - s3
                - gcs
                type: string
              managedClusterName:
                description: ManagedClusterName refers to the HumioCluster that owns
                  this dependency check
                minLength: 1
                type: string
              nodePoolName:
                description: NodePoolName is the node pool this check belongs to,
                  empty for default pool
                type: string
            required:
            - checkType
            - managedClusterName
            type: object
          status:
            description: HumioDependencyCheckStatus defines the observed state of
              HumioDependencyCheck
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the resource's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              configuredEnvVars:
                description: ConfiguredEnvVars lists the environment variables that
                  were forwarded for this check
                items:
                  type: string
                type: array
              discoverySource:
                description: DiscoverySource indicates how this check was discovered
                  (e.g. "auto")
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
